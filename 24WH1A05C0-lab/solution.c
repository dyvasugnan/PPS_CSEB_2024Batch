#include<stdio.h>
#include<math.h>
int main(){
int x;
float sol;
printf("enter the value of x");
scanf("%d",&x);
sol=(1-(x/2)+(sqrt(x))-(sqrt(x)));
printf("The solution of given equation is:%f",sol); 
return 0;
}
